
$(document).ready(function () {
    // Los campos ya tienen el atributo 'required' en HTML.
    // El navegador se encargará de mostrar los avisos si falta algo.
    // Hemos eliminado la restricción de deshabilitar el botón para mayor compatibilidad.
});